// ASP.NET Core backend compatibility objects.
// All reads/writes are routed through the ASP.NET Core API. All reads/writes are routed through src/lib/api.ts.
export { auth, db } from "@/src/lib/backendCompat";
